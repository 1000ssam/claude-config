# wee-log 출시 전 QA 체크리스트 — 2026-06-12

> main(55b7388)에 **세 묶음이 한꺼번에** 올라가 있고 시각 QA만 남음.
> 묶음: ① audit 리팩토링(phase0-2·phase3) ② 원격 LLM 레벨1 UI ③ family-look(링글 브랜딩·대시보드·폰트)
> +④ **동의서 감사 수정**(`feat/consent-audit-fixes` @5d96662, 17ecbfb 기반 — p4 브랜치 머지 후 함께 머지) → 4번 섹션

## ✅ 이미 검증됨 (재확인 불필요)
- tsc(web+node) 0에러 · 단위체크 21/21 · eslint 0에러 · `npm run build` 성공 — **합쳐진 main에서 통과 확인(WSL)**.
- ⚠️ Windows에서 첫 실행 전: `npm i --no-save @rollup/rollup-win32-x64-msvc` 필요할 수 있음(npm#4828), better-sqlite3는 PowerShell에서 `npx @electron/rebuild`.

## 1. 리팩토링 회귀 (최우선 — 동작 보존 리팩토링이라 깨지면 티 안 남)
- [ ] **학생 페이지**: 목록 로드 / 검색 / 필터 / 정렬 / 페이지네이션 / 학생 추가·수정·삭제 / 엑셀 업로드
- [ ] **통계 페이지**: 차트 렌더 / 기간·필터 변경 시 갱신 / 빈 데이터 상태
- [ ] **캘린더**: 월/주 전환 / 이벤트·공휴일·학사일정 표시 / 드래그 이동 / 일정 클릭
- [ ] **설정 페이지**: 전 항목 저장·재로드 / 컬러 팔레트 변경 즉시 반영
- [ ] **회기 저장 → 자동 한 줄 요약**(로컬 AI) 동작
- [ ] 사례 상세 / 보고서 / 동의서 등 주요 화면 스모크

## 2. 원격 LLM UI (신규 기능)
- [ ] 설정 → AI 도우미에 **"원격 서버 사용" 토글** 노출
- [ ] 토글 ON → **고지 다이얼로그** 표시 → **취소** 시 토글 안 켜짐
- [ ] 동의 → 원격 카드(주소·모델·API키·컨텍스트 길이)로 전환
- [ ] 토글 OFF → 기존 모델 다운로드 카드로 복귀 + **로컬 AI 생성 1회 정상**
- [ ] (서버 없으면) "연결 확인" 실패 시 한국어 에러 토스트 — 앱 안 죽음
- [ ] 원격 카드에서 **API 키가 화면에 평문 노출 안 됨**(password 입력)

## 3. family-look (시각)
- [ ] 앱명·브랜딩 "링글" 일관 표시(타이틀·아이콘·about)
- [ ] 홈 대시보드 재구성 레이아웃 정상(목업 합의안 대비)
- [ ] **폰트 자체 호스팅**: Inter·Pretendard·Jua가 오프라인(네트워크 끊고)에서도 정상 렌더 — CDN 의존 제거 확인
- [ ] 색/여백 깨짐 없음, 다크/라이트(있다면) 정상

## 4. 동의서 감사 수정 — ✅ main 머지됨(e809bba, 2026-06-12). 폼 항목 라이브 3/3 통과, flow 34/34
> ✅ ~~프로덕션 Worker 재배포 대기~~ — 2026-06-13 배포·라이브 검증 완료(HANDOFF-consent.md 참조).
> 아래 패널 항목(4-1~)은 머지된 앱(`/mnt/c/dev/wee-log`, 이제 main)에서 육안 — 후속.
> 감사 리포트: `wee-log-consent-audit-2026-06-12.md` (보안7·워크플로12·UX12)
- [x] **발송 다이얼로그**: 같은 학생·양식 재발송 시 "기존 링크 즉시 만료" 경고 박스 표시 — ✅ 2026-06-13 육안 검증·오발 버그 수정(f60a2a3)
- [x] **에러 문구**: 서버 미설정 배너에 개발자 용어(VITE_…) 안 보임 / SMS·서버 오류가 한국어 안내 — ✅ 2026-06-13 검증·구체 사유 노출 개선(9300195)
- [ ] **보호자 폼 (모바일 브라우저)**:
  - [ ] 정상 로드·작성·제출 — **CSP 추가 후 libsodium 암호화가 정상 동작하는지** (가장 중요한 회귀 포인트)
  - [ ] 비행기모드로 열기 → 12초 후 "페이지를 불러오지 못했습니다" 폴백 (영구 '불러오는 중' 아님)
  - [ ] 작성 중 다른 기기에서 재발송 → 제출 시 입력 보존된 채 만료 안내(화면 안 지워짐)
- [ ] **자동 수합**: 발송 후 동의서 탭 안 들어가도 15분 내 백그라운드 수합됨(잠금 해제 상태)
- [ ] 실기기(iOS Safari·삼성 인터넷) 서명 캔버스 드로잉·화면 회전 좌표 (감사에서 코드로 단정 불가 항목)

## 5. 동의서 일괄 발송 + 기본 전체 체크 — 🆕 `feat/consent-case-batch`(3a39a71, main 미머지)
> WSL 검증 완료: tsc(web/node) 0 · eslint 0 · test:units 22스위트(신규 consent-send-defaults 24 + resend-scope 14).
> 아래는 Windows 앱에서 육안. 사례에 **학생 2명 이상 + 양식 2개 이상** 등록해 두고 시작.
- [ ] **기본 전체 체크**: 미발송 상태에서 발송 다이얼로그 열면 **모든 양식이 체크**되어 있고 경고 박스 없음
- [ ] **상태 배지**: 발송된 양식은 기본 해제 + "발송됨 · 응답 대기" 배지 / 응답 수합된 양식은 "응답 완료" 배지
- [ ] **재발송 경고**: 발송됨 양식을 직접 체크해야만 경고 박스 표시(건수 = 학생×양식 쌍 단위)
- [ ] **전체 발송 버튼**: 학생 2명 이상 사례에서만 "전체 학생에게 한 번에 발송" 노출(1명이면 미노출)
- [ ] **다학생 다이얼로그**: 학생 체크리스트 기본 전원 체크 / SMS 채널에서 연락처 없는 학생은 비활성+"연락처 없음 — SMS 제외" 표기
- [ ] **채널 전환**: SMS↔링크 전환 시 연락처 없는 학생 선택이 자동 제외/재활성화
- [ ] **발송 버튼 라벨**: "문자 발송 (학생 n명 · m건)" 건수 정확 / 30건 초과 시 한도 경고문
- [ ] **링크 다이얼로그(다학생)**: 학생별 그룹 헤더 + 양식명 라벨 + "전체 링크 한 번에 복사"가 `[학생명]` 헤더 형식
- [ ] **SMS 실발송**: 학생 2명 × 양식 2개 → 보호자 번호별로 **문자 1통씩(각 2링크)** 수신 확인
- [ ] **부분 실패 토스트**: 일부 실패 시 "성공 n건 · 실패 m건 (· 연락처 없음 제외 k명)" 요약
- [ ] **단일 학생 회귀**: 기존 단일 발송 플로우(다이얼로그 외관·링크 1건 표시·문자 1통 묶음) 변화 없음

## 판정
- 1번에서 회귀 발견 시 → 해당 영역 핀포인트 후 수정(리팩토링 산출물 점검)
- 2·3번은 신규/시각 → 어색하면 조정. 치명적이지 않으면 출시 후 패치 가능.
- 5번 통과 시 → 사용자 "머지해" 지시 후 main 머지(브랜치 정책).
- 전부 OK면 출시 게이트(P4: Solapi 서버사이드·Worker 인증·좌석캡)로 이동.
  - P4 작업에 전달할 감사 발견: **S-1**(Solapi 키 빌드 인라인), **S-2**(/requests 무인증, bearer 회전 시 레이트리밋 우회), **W-4**(entitlement 클라 헤더 미송신 — 플래그 켜면 전 기능 401 마비, Worker에 SMS 발송 라우트 부재) — 리포트 D 묶음 참조.
